# Lixo de SO / editor
Thumbs.db
desktop.ini
.DS_Store
.thumbnail

# Config local do Claude Code (o launch.json fica versionado de propósito)
.claude/settings.local.json

# Backups locais do editor
*.bak
*.bak[0-9]

# Pacote de deploy manual da v3 no Cloudflare (direct upload). E gerado a
# partir de v3.html + assets, entao versionar so duplicaria as imagens.
deploy-v3/
nex-v3-cloudflare.zip
